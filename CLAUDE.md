# Project Rules

- Never use `cd`, `&&`, or absolute paths in shell commands
- Use `source ~/.bashrc` before running `aliyun` commands (PATH not auto-loaded)
- `deploy` means: `railway up -d -c -m "<latest commit message>"`
- Use `npx kill-port <port>` to kill processes
